[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hopper"
version = "0.3.0"
description = "Persistent memory and task management for AI agents"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "Hopper Team"},
]
keywords = ["task-routing", "orchestration", "ai-agents", "mcp"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    # CLI framework
    "click>=8.1.7",
    "rich>=13.7.0",
    # Config and validation
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    # Local storage
    "pyyaml>=6.0.1",
    # HTTP client (server-mode CLI, MCP bridge, GitHub platform)
    "httpx>=0.25.0",
    # MCP server (primary use case for Claude Desktop / Claude Code)
    # Requires >=1.0.0 for SSE transport support (includes starlette, sse-starlette)
    "mcp>=1.0.0",
    # DID signing for upstream sync
    "pynacl>=1.5.0",
]

[project.optional-dependencies]
server = [
    # HTTP server
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    # Database ORM and migrations
    "sqlalchemy>=2.0.0",
    "alembic>=1.12.0",
    "psycopg2-binary>=2.9.9",
    # Auth / security middleware
    "python-jose[cryptography]>=3.3.0",
    "passlib[bcrypt]>=1.7.4",
    "python-multipart>=0.0.6",
    # Working memory backend
    "redis>=5.0.0",
    # Episodic / semantic memory
    "opensearch-py>=2.4.0",
    # LLM routing (Phase 4 — placeholder until wired)
    "anthropic>=0.7.0",
]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "black>=23.11.0",
    "ruff>=0.1.6",
    "mypy>=1.7.0",
    "types-pyyaml",
    "types-redis",
]

[project.scripts]
hopper = "hopper.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 100
target-version = ["py311"]
include = '\.pyi?$'

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_calls = true
disallow_untyped_decorators = false
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
follow_imports = "normal"
strict_equality = true

[[tool.mypy.overrides]]
# Server-extra and optional packages — not present in a core install
module = [
    "opensearch_py",
    "anthropic",
    "fastapi",
    "sqlalchemy",
    "alembic",
    "redis",
    "jose",
    "passlib",
    "mcp",
    "nacl",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=hopper",
    "--cov-report=term-missing",
]
asyncio_mode = "auto"
